# Server configuration
server.port=8080

# Application name
spring.application.name=spam-classifier-backend

# Flask ML service URL (change if deployed remotely)
flask.service.url=http://localhost:5000

# Logging
logging.level.com.spamclassifier=DEBUG
logging.level.org.springframework.web=INFO
